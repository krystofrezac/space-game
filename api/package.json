{
  "name": "api",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": " rimraf dist  && tsc",
    "watch-ts": "tsc -w",
    "watch-node": "nodemon dist/main",
    "watch": "yarn build && concurrently -k -n \"TypeScript, Node\" -c \"yello.bold, cyan.bold\" \"yarn run watch-ts\" \"yarn run watch-node\""
  },
  "dependencies": {
    "rimraf": "^3.0.2",
    "socket.io": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  }
}
