const config = {
  delta: 1000 / 60,
  angularAcceleration: 0.0005,
  maxAngularSpeed: 0.05,
  maxSpeed: 10,
  acceleration: 1,
  visibleArea: {
    width: 1600,
    height: 800,
  },
};

export default config;
